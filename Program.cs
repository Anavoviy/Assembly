using System;
using Assemblies;

class Program
{
    public static void Main()
    {
        /*CPU cpu = new();
        MotherBoard MB = new();
        RAM ram = new();
        GPU gpu = new();
        CPUCooling cpuCooling = new();

        DataStorageDevice SSD = new();
        DataStorageDevice HDD = new();
        List <DataStorageDevice> dataStorageDevices = new List<DataStorageDevice>();
        dataStorageDevices.Add(SSD);
        dataStorageDevices.Add(HDD);
        
        PowerSupply PS = new();
        ComputerCase computerCase = new();*/

        /*Assembly assembly = new Assembly("Гор", (ulong)75000, cpu, MB, ram, gpu, cpuCooling, dataStorageDevices, PS, computerCase);*/


        //Алгоритм работы системы по сборке и подбору совместимого оборудования
        //1) Подключение к базе данных

        //2) Вывод пользователю окно сборки с пустыми полями комплектующих
        //2.1) Создание пустого экземпляра сборки
        
        //3) Выбор пользователем первого комплектующего
        //3.1) Добавление комплектующего в поле на окне сборки
        //3.2) Добаление комплектующего в экземпляр сборки
        
        //4) Выбор пользователем второго комплектующего
        //*программа даёт выбрать только совместимое комплектующее, основываясь на уже выбранном/выбранных ранее комплектующих
        //4.1) Добавление комплектующего в поле окна сборки
        //4.2) Добавление комплектующего в экзеампляр сборки

        //5...10) Выбор остальных комплектующих по алгоритму (4)

        //11) Выставление общего счёта за комплектующие
        //11.1) Предложение по разным услугам сборки/настройки ПК
        //11.2) Подсчёт общей стоимости устройства вместе с работой по сборке/настройке ПК



        

    }

}